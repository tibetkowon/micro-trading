# ── KIS (한국투자증권) API ────────────────────────────────────────────────────
KIS_APP_KEY=
KIS_APP_SECRET=
KIS_ACCOUNT_NO=        # 계좌번호 앞 8자리만 입력 (예: 12345678)
KIS_ACCOUNT_TYPE=01    # 01=종합계좌, 22=선물옵션
KIS_BASE_URL=https://openapi.koreainvestment.com:9443
# 모의투자: KIS_BASE_URL=https://openapivts.koreainvestment.com:29443

# KIS HTS ID — 실시간 체결통보(H0STCNI0) 구독 시 필요. 미설정 시 체결통보 미구독.
KIS_HTS_ID=

# ── Firebase / Firestore ──────────────────────────────────────────────────────
FIREBASE_PROJECT_ID=                          # GCP 프로젝트 ID (예: micro-trading-cb6f5)
FIREBASE_CREDENTIALS_JSON=./firebase-credentials.json  # 서비스 계정 JSON 파일 경로

# ── 서버 설정 ─────────────────────────────────────────────────────────────────
SERVER_PORT=8080
FRONTEND_DIST_PATH=./frontend/dist            # 로컬 개발용 (프로덕션은 Firebase Hosting)

# ── CORS ─────────────────────────────────────────────────────────────────────
FRONTEND_ORIGIN=https://YOUR_PROJECT_ID.web.app  # Firebase Hosting 도메인

# ── Slack 알림 ────────────────────────────────────────────────────────────────
# 각 값에는 Slack Incoming Webhook URL을 입력합니다.
# 비워두면 해당 알림은 SLACK_WEBHOOK_URL로 fallback됩니다.
SLACK_WEBHOOK_URL=                 # 기본/fallback 채널
SLACK_ALERT_WEBHOOK_URL=           # WARN/ERROR 로그 알림 채널
SLACK_KIS_WEBHOOK_URL=             # KIS API 연속 실패 알림 채널
SLACK_TRADE_WEBHOOK_URL=           # 매수/매도 체결 알림 채널
SLACK_POSITION_WEBHOOK_URL=        # 보유 포지션 현황 스냅샷 채널
SLACK_KIS_FAIL_THRESHOLD=5         # KIS API 연속 실패 N회마다 알림
SLACK_POSITION_SNAPSHOT_MIN=30     # 장중 포지션 스냅샷 발송 간격(분)
